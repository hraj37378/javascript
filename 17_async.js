// Javascript is a synchronous and single threaded language

// Execution context -> executes one line of code at a time . Each operation wait for the last one to complete before executing.

// Call Stack AND Memory Heap

// Blocking Code VS Non Blocking Code

// Blocking the flow of program Vs Does not block execution

// Read file Sync Vs Read File Async 


// "async and await make promises easier to write"

// async makes a function return a Promise

// await makes a function wait for a Promise

// The keyword async before a function makes the function return a promise.
